# DIGITAL-CLOCK
A simple C console **digital clock** that shows the current local time in HH:MM:SS format and updates every second. Uses `time.h` with `time()` and `localtime()`, a loop, and a delay to refresh the same line like a real-time terminal clock.
